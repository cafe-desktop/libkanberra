##########################################################
# THE FOLLOWING LINES IS USED BY docker-build
##########################################################
requires:
  debian:
    - aptitude
    - clang-tools
    - cppcheck
    - expect #required for unbuffer
    - git
    - gnome-pkg-tools
    - ctk-doc-tools
    - libltdl-dev
    - libasound2-dev
    - libvorbis-dev
    - libctk-3-dev
    - libctk2.0-dev
    - libtdb-dev
    - libpulse-dev
    - libgstreamer1.0-dev

before_scripts:
  - ./html-cppcheck

build_scripts:
  - ./before-build
  - ./make-scanbuild
  - ./after-build

after_scripts:
  - ./gen-index -l 20
